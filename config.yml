title: Full stack
description: 'I have started to learn Full-Stack, I post my codes throughout my study.'
template:
  repo: full-stack-template
  name: full-stack
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
